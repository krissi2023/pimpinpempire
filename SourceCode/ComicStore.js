class ComicStore { constructor() { this.packages = new Map(); this.purchases = new Map(); this.initializePackages(); } initializePackages() { this.packages.set("diamond-heist-starter", { id: "diamond-heist-starter", name: "Diamond Heist Starter Pack", price: 4.99, contents: { goldCoins: 500, ppcCoins: 100 } }); } getAllPackages() { return Array.from(this.packages.values()); } getPackage(packageId) { return this.packages.get(packageId); } processSuccessfulPurchase(paymentIntentId, packageId, userId) { const packageData = this.getPackage(packageId); if (!packageData) throw new Error(`Package ${packageId} not found`); return { id: "purchase_" + Date.now(), packageId, userId, packageData }; } } module.exports = ComicStore;
